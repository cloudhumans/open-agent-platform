# Open Agent Platform

## Required Claude Code Skills

Install the `ui-ux-pro-max` skill before working on this repo:

```bash
mkdir -p .claude/skills && git clone https://github.com/anthropics/ui-ux-pro-max .claude/skills/ui-ux-pro-max
```

The `.claude/skills/` directory is gitignored — each dev must install locally.
