apiVersion: v1
kind: Service
metadata:
  name: open-agent-platform
  namespace: open-agent-platform
  labels:
    app: open-agent-platform
spec:
  ports:
    - port: 80
      targetPort: 3000
      protocol: TCP
      name: http
  selector:
    app: open-agent-platform
